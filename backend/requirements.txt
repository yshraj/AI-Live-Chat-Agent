# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Database & Cache
pymongo==4.6.0  # Includes bson module
redis==5.0.1

# AI & ML
google-genai>=0.2.0  # Google Gemini SDK (new package) - REQUIRED
# Note: google-generativeai is deprecated, but kept as fallback. Install google-genai instead.
# Embeddings: Using Hugging Face Inference API (free, no local model needed)
# No need for sentence-transformers, transformers, torch, or huggingface-hub
numpy==1.24.3  # Only needed for cosine similarity calculations

# Configuration
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
