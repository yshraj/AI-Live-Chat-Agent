# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Database & Cache
pymongo==4.6.0  # Includes bson module
redis==5.0.1

# AI & ML
google-genai>=0.2.0  # Google Gemini SDK (new package) - REQUIRED
# Note: google-generativeai is deprecated, but kept as fallback. Install google-genai instead.
sentence-transformers>=2.3.0,<3.0.0  # Compatible with modern huggingface-hub
huggingface-hub>=0.20.0  # Required for sentence-transformers 2.3+
transformers>=4.21.0  # Required by sentence-transformers
numpy==1.24.3
# Use CPU-only PyTorch to reduce memory footprint (saves ~200-300MB)
# Install with: pip install torch --index-url https://download.pytorch.org/whl/cpu
# For production, we'll install CPU-only torch in build command
torch>=1.13.0,<2.0.0  # Required by sentence-transformers (CPU-only version recommended)

# Configuration
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
