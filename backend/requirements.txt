# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Data Validation
pydantic==2.5.0
pydantic-settings==2.1.0

# Database & Cache
pymongo==4.6.0  # Includes bson module
redis==5.0.1

# AI & ML
google-genai>=0.2.0  # Google Gemini SDK (new package) - REQUIRED
# Note: google-generativeai is deprecated, but kept as fallback. Install google-genai instead.
sentence-transformers>=2.3.0,<3.0.0  # Compatible with modern huggingface-hub
huggingface-hub>=0.20.0  # Required for sentence-transformers 2.3+
transformers>=4.21.0  # Required by sentence-transformers
numpy==1.24.3
# PyTorch installation:
# - For LOCAL development: Install normally from PyPI (pip install torch>=2.0.0)
# - For PRODUCTION (Render): CPU-only torch is installed automatically via build command
# Note: CPU-only PyTorch repo only has versions >= 2.0.0, so we use >=2.0.0 here
torch>=2.0.0  # Compatible with both PyPI and CPU-only repository

# Configuration
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
